# ⚠️ DEPRECATED: This file is no longer used
# All requirements have been moved to infra/docker/requirements.txt
# 
# Use Docker to run the project:
#   docker-compose up
#
# For specific services:
#   docker-compose --profile analytics up analytics
#   docker-compose --profile scripts up scripts
#   docker-compose --profile crawling up crawling
#
# See infra/docker/requirements.txt for all dependencies