apps/ 아래가 “실제 돌아가는 서비스 단위”

backend = Django API

frontend = React UI

rag = LangGraph 기반 Q&A/상담 추천

reco = 모델 학습/서빙(신뢰도+시세+로그 추천)

libs/ 는 여러 앱이 같이 쓰는 코드(특히 DB/스키마/외부 API 클라이언트)
→ 백/랭그래프/모델에서 동일 코드 중복 방지

infra/ 가 운영 환경(도커/DB/Neo4j/nginx)
→ docker-compose에서 각 컨테이너가 여기 설정을 읽음

scripts/ 가 ETL/배치/갱신

매물 수집

임베딩 생성/업데이트

Neo4j 그래프 동기화

로그 백필 등